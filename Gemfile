source "https://rubygems.org"

# Specify your Ruby version
ruby '2.7.2'  # Adjust the version number as needed

gem "jekyll", "~> 4.2.0"
gem "webrick", "~> 1.7"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
  # Specify the version if possible, here is an example, check for the latest compatible version
  gem "jekyll-multiple-languages-plugin", "~> 1.6.1"
end

# Add development specific gems here
group :development do
  # Add gems like 'jekyll-admin' if used locally for easier site management
  gem "jekyll-admin", "~> 0.11.0"
end

